# Server connection
server:
  hostname: openl10n.dev
  username: user
  password: user

# Project specification
project: silex

# Translation files
files:
  -
    pattern: resources/locales/<locale>.yml
    #exclude: countries.en.yml
    options:
      pull: [fallback_locale, fallback_key]

# Misc options
options: ~
